---
# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: 2024 The Linux Foundation <https://linuxfoundation.org>

name: 'Nexus IQ Scan'

# yamllint disable-line rule:truthy
on:
  # ToDo: convert to a reusable workflow later
  workflow_dispatch:
    # workflow_call:
    #    inputs:
    ### Required when interacting with Gerrit
    #      GERRIT_BRANCH:
    #        description: 'Branch that change is against'
    #        required: true
    #        type: string
    #      GERRIT_CHANGE_ID:
    #        description: 'The ID for the change'
    #        required: true
    #        type: string
    #      GERRIT_CHANGE_NUMBER:
    #        description: 'The Gerrit number'
    #        required: true
    #        type: string
    #      GERRIT_CHANGE_URL:
    #        description: 'URL to the change'
    #        required: true
    #        type: string
    #      GERRIT_EVENT_TYPE:
    #        description: 'Type of Gerrit event'
    #        required: true
    #        type: string
    #      GERRIT_PATCHSET_NUMBER:
    #        description: 'The patch number for the change'
    #        required: true
    #        type: string
    #      GERRIT_PATCHSET_REVISION:
    #        description: 'The revision sha'
    #        required: true
    #        type: string
    #      GERRIT_PROJECT:
    #        description: 'Project in Gerrit'
    #        required: true
    #        type: string
    #      GERRIT_REFSPEC:
    #        description: 'Gerrit refspec of change'
    #        required: true
    #        type: string

    ### Specific to this workflow
    SOURCE-ORIGIN:
      description: 'Code repository/origin [gerrit|other]'
      required: false
      type: string
      default: 'gerrit'

concurrency:
  # yamllint disable-line rule:line-length
  group: composed-node-verify-clm${{ github.workflow }}-${{ github.event.inputs.GERRIT_CHANGE_ID || github.run_id }}
  cancel-in-progress: true

jobs:
  #prepare:
  #  runs-on: ubuntu-latest
  #  steps:
  #    - name: Clear votes
  #      if: inputs.source-origin == 'gerrit'
  #      # yamllint disable-line rule:line-length
  #      uses: lfit/gerrit-review-action@7c30179c3c9389545fccb0d458df59879372ae6a # v0.6
  #      with:
  #        username: ${{ vars.GERRIT_SSH_USER }}
  #        host: ${{ vars.GERRIT_SERVER }}
  #        key: ${{ secrets.GERRIT_SSH_PRIVKEY }}
  #        known_hosts: ${{ vars.GERRIT_KNOWN_HOSTS }}
  #        gerrit-change-number: ${{ inputs.GERRIT_CHANGE_NUMBER }}
  #        gerrit-patchset-number: ${{ inputs.GERRIT_PATCHSET_NUMBER }}
  #        vote-type: clear
  #        comment-only: true
  #    - name: Allow replication to GitHub
  #      run: sleep 10s

  node-verify-clm:
    runs-on: ubuntu-latest
    #needs: [prepare]
    steps:
      # Below needs to be swapped for GERRIT change/checkout for VERIFY jobs (NOT merge)
      - uses: actions/checkout@v4
        if: inputs.source-origin != 'gerrit'
      # Prevents the need to pass GERRIT requirements into subsequently called actions/workflows
      #- name: Export env variables
      #  id: export-env-variables
      #  uses: infovista-opensource/vars-to-env-action@1.0.0
      #  with:
      #    secrets: ${{ inputs.env-vars }}
      #- name: Export env secrets
      #  id: export-env-secrets
      #  uses: infovista-opensource/vars-to-env-action@1.0.0
      #  with:
      #    secrets: ${{ inputs.env-secrets }}

      # Build Node.js project
      - uses: ModeSevenIndustrialSolutions/portal-ng-ui/.github/actions/node-build@master
        with:
          node-version: 20
          build-tool: yarn

      - name: Nexus IQ Policy Evaluation
        # Somebody has set continue-on-error in the RelEng Reusable Workflows Repository
        # continue-on-error: true
        # Why?!?!?
        # yamllint disable-line rule:line-length
        # See: https://github.com/lfit/releng-reusable-workflows/blob/main/.github/workflows/composed-gradle-nexus-iq.yaml
        # yamllint disable-line rule:line-length
        # See: https://github.com/lfit/releng-reusable-workflows/blob/main/.github/workflows/composed-maven-nexus-iq.yaml
        # This has multiple issues:
        # uses: sonatype-nexus-community/iq-github-action@main
        # See: https://github.com/onap/portal-ng-ui/actions/runs/10527123076/job/29169584344
        # e.g. /entrypoint.sh: 18: [[: not found
        uses: ModeSevenIndustrialSolutions/iq-github-action@main
        env:
          stage: 'build'
        with:
          serverUrl: ${{ vars.NEXUS_IQ_SERVER }}
          username: ${{ vars.NEXUS_IQ_USERNAME }}
          password: ${{ secrets.NEXUS_IQ_PASSWORD }}
          #applicationId: ${{ github.repository_owner }}-${{ github.event.repository.name }}
          # Temporarily force to ONAP because testing in ModeSevenIndustrialSolutions
          applicationId: onap-${{ github.event.repository.name }}
          stage: 'build'
          # target: ''
          writePolicyEvaluationJson: 'policy-eval-results.json'
          # debug: true

  #vote:
  #  if: inputs.source-origin == 'gerrit'
  #  needs: [prepare, node-verify-clm]
  #  runs-on: ubuntu-latest
  #  # yamllint enable rule:line-length
  #  steps:
  #    - name: 'Get conclusion'
  #      # yamllint disable-line rule:line-length
  #      uses: technote-space/workflow-conclusion-action@45ce8e0eb155657ab8ccf346ade734257fd196a5 # v3.0.3
  #    - name: 'Set vote'
  #      # yamllint disable-line rule:line-length
  #      uses: lfit/gerrit-review-action@7c30179c3c9389545fccb0d458df59879372ae6a # v0.6
  #      with:
  #        host: ${{ vars.GERRIT_SERVER }}
  #        username: ${{ vars.GERRIT_SSH_USER }}
  #        key: ${{ secrets.GERRIT_SSH_PRIVKEY }}
  #        known_hosts: ${{ vars.GERRIT_KNOWN_HOSTS }}
  #        gerrit-change-number: ${{ inputs.GERRIT_CHANGE_NUMBER }}
  #        gerrit-patchset-number: ${{ inputs.GERRIT_PATCHSET_NUMBER }}
  #        vote-type: ${{ env.WORKFLOW_CONCLUSION }}
  #        comment-only: true
